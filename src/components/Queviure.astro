---
import { Delete } from "lucide-astro";
import { actions } from "astro:actions";

interface Props {
  nom: string;
}
const { nom } = Astro.props;
---

<div class="w-full h-fit flex flex-row justify-between">
  <span>{nom}</span>
  <form action={actions.borraQueviure} method="POST">
    <label>
      <input hidden name="nom" required value={nom} />
    </label>
    <button type="submit" class="text-red-500 my-auto">
      <Delete class="size-8 stroke-1" />
    </button>
  </form>
</div>
